/**
 * input — React 组件
 * 
 * 所属目录：registry
 */
import type { ComponentEntry } from './types'

// Intentionally empty: input playground samples moved to canonical Chat Inputs entries
// (InputContainer and Slash Command Demo) to avoid duplicate maintenance.
/** inputComponents：常量 */
export const inputComponents: ComponentEntry[] = []
